apiVersion: 1

datasources:
  - name: InfluxDB_v2_Flux
    type: influxdb
    access: proxy
    url: http://tema3_db:8086
    secureJsonData:
      token: sprc_token
    jsonData:
      version: Flux
      organization: sprc_org
      defaultBucket: tema3_db
      tlsSkipVerify: true

